# Source location
$Source = "C:\Temp\2026 Vias institute colours.thmx"

# Destination
$Destination = "$env:APPDATA\Microsoft\Templates\Document Themes"

# Create folder if needed
if (!(Test-Path $Destination)) {
    New-Item -ItemType Directory -Path $Destination -Force
}

# Copy theme
Copy-Item $Source -Destination -Force
